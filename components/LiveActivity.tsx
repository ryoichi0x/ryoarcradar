import { activities } from "@/lib/mock-data";
const dots = { green: "bg-accent", blue: "bg-blue-400", orange: "bg-orange-400" };
export function LiveActivity() { return <section className="panel p-5 sm:p-6"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Live activity</h2><p className="mt-1 text-xs text-muted">Recent events across Arc</p></div><span className="rounded-full bg-accent/10 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-accent">Mock feed</span></div><div className="mt-6 space-y-5">{activities.map((item) => <div key={item.title} className="flex gap-3"><span className={`mt-1.5 h-2 w-2 shrink-0 rounded-full ${dots[item.tone]}`} /><div className="min-w-0 flex-1"><div className="flex flex-col justify-between gap-1 sm:flex-row"><p className="text-sm font-medium">{item.title}</p><span className="text-xs text-muted">{item.time}</span></div><p className="mt-1 truncate text-xs text-muted">{item.detail}</p></div></div>)}</div></section>; }
