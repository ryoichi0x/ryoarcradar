import { trendingTokens } from "@/lib/mock-data";
export function TrendingTokens() { return <section className="panel p-5 sm:p-6"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Trending tokens</h2><p className="mt-1 text-xs text-muted">Highest momentum · mock data</p></div><button className="text-xs font-semibold text-accent hover:underline">View all →</button></div><div className="mt-5 space-y-4">{trendingTokens.map((token) => <div key={token.symbol} className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-full text-sm font-bold text-ink" style={{ backgroundColor: token.color }}>{token.symbol.slice(0, 1)}</span><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{token.name}</p><p className="text-xs text-muted">{token.symbol}</p></div><div className="text-right"><p className="text-sm font-semibold">{token.price}</p><p className="text-xs text-accent">{token.change}</p></div></div>)}</div></section>; }
